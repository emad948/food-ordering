import { MUIDataTableOptions } from 'mui-datatables';

import textLabels from './datatables-options';

const config: MUIDataTableOptions = {
  textLabels,
  download: false,
  print: false,
};

export default config;
